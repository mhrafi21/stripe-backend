{
  "name": "stripe-backend",
  "version": "1.0.0",
  "description": "Backend for Stripe integration with Express",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:dev": "nodemon index.js",
    "start": "node index.js",
    "build": "echo 'No build step required for backend'",
    "test": "echo \"Error: no test specified\" && exit 1"

  
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "stripe": "^18.1.1"
  }
  
}
